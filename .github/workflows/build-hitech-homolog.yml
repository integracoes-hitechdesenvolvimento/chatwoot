name: Build and publish Chatwoot HiTech homolog image

on:
  push:
    branches:
      - hitech-custom-v4.13.0
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    timeout-minutes: 180

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push Docker image
        id: build
        uses: docker/build-push-action@v6
        with:
          context: .
          file: docker/Dockerfile
          push: true
          provenance: false
          sbom: false
          cache-from: type=gha,scope=hitech-homolog
          cache-to: type=gha,mode=max,scope=hitech-homolog
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:hitech-homolog
            ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:hitech-v4.13.0
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Print published image tags
        run: |
          echo "Published:"
          echo "  ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:hitech-homolog"
          echo "  ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:hitech-v4.13.0"
          echo "Digest: ${{ steps.build.outputs.digest }}"
